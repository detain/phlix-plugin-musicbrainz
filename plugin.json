{
    "name": "phlix-plugin-musicbrainz",
    "version": "0.4.0",
    "phlix_min_server_version": "1.2.0",
    "type": "metadata-provider",
    "entry": "Phlix\\Plugin\\MusicBrainz\\MusicBrainzPlugin",
    "events": [
        "phlix.library.item.added"
    ],
    "settings": {
        "enabled": {
            "type": "boolean",
            "default": false,
            "required": false,
            "tier": "standard",
            "label": "Enable MusicBrainz",
            "description": "Master on/off for MusicBrainz enrichment. Optional; default off. No API key needed — MusicBrainz is an open service."
        },
        "user_agent": {
            "type": "string",
            "default": "PhlixMusicBrainzPlugin/0.1.0 (https://github.com/detain/phlix-plugin-musicbrainz)",
            "required": false,
            "tier": "advanced",
            "label": "User-Agent",
            "description": "Identifies Phlix to MusicBrainz (their policy REQUIRES a descriptive User-Agent with contact info, e.g. a URL). Optional — the default is fine; customise if you run a fork.",
            "link": "https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting",
            "link_text": "MusicBrainz API etiquette"
        },
        "rate_limit_delay": {
            "type": "integer",
            "default": 1100,
            "required": false,
            "tier": "advanced",
            "label": "Rate-limit delay (ms)",
            "description": "Minimum delay between MusicBrainz requests. Optional; default 1100 (MusicBrainz asks for no more than ~1 request/second)."
        },
        "auto_enrich": {
            "type": "boolean",
            "default": true,
            "required": false,
            "tier": "advanced",
            "label": "Auto-enrich",
            "description": "Automatically enrich music items as they are scanned. Optional; default on."
        },
        "fetch_album_art": {
            "type": "boolean",
            "default": true,
            "required": false,
            "tier": "advanced",
            "label": "Fetch album art",
            "description": "Pull cover art via the Cover Art Archive. Optional; default on."
        },
        "search_depth": {
            "type": "string",
            "default": "normal",
            "required": false,
            "tier": "advanced",
            "label": "Search depth",
            "description": "How hard to search for matches: fast, normal or deep. Optional; default normal."
        }
    }
}
